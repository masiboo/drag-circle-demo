# ===============================
# PostgreSQL Configuration
# ===============================

spring.datasource.url=jdbc:postgresql://localhost:5432/circle_db
spring.datasource.username=user
spring.datasource.password=Finbd786
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


# Optional PostgreSQL-specific dialect

# Enable SQL stats/logs for debugging
spring.jpa.properties.hibernate.generate_statistics=true
logging.level.org.hibernate.type=trace
logging.level.org.hibernate.stat=debug

# Enable actuator-style endpoint logging
logging.level.org.springframework.web.servlet.mvc.method.annotation=TRACE
